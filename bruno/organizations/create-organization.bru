meta {
  name: Create Organization
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/orgs/{{orgId}}
  body: json
  auth: none
}

params:path {
  orgId: {{$randomUUID}}
}

body:json {
  {
    "name": "Acme Corp",
    "slug": "acme-corp"
  }
}
